# handoff-07/07/26,10:01

**Two things shipped this session: (1) an app-state review that fixed 6 findings, and (2) the Phase 5 nightly-incremental orchestrators — the last major pre-season build. Both are committed on branch `phase5-nightly-incremental` and opened as [PR #1](https://github.com/Joshc386/BetStatsInterface/pull/1). This project is now on GitHub for the first time** (new **private** repo `Joshc386/BetStatsInterface`; default branch is `master`, there is no `main`). Nothing is mid-flight. PR #1 is unmerged — reviewing/merging it is the user's call.

## One-line context
Personal football betting-*research* tool: free data → local Postgres → read-only FastAPI → React (Vite). Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md`, decisions in `docs/adr/`. **Read `memory/*.md` first** — esp. `phase5-nightly-incremental.md` (what was just built), `post-backfill-roadmap.md` (what's next), `cups-internationals-sourcing.md` (ops lessons + internationals scope).

## What shipped today (commits/PR carry the detail — don't re-derive)
- **`f8a247b`** `fix: Europe squad-form scope + coverage-honesty polish` — the 6 app-review findings: Europe scope added to squad form (`SquadForm.tsx`); `GET /competitions` now returns only competitions with ingested rows (hides deferred UEFA Super Cup, auto-surfaces future leagues — `main.py`); scope-aware empty state, venue-aware panel titles, "spans N seasons" hint, mobile venue-panel stacking (`FixtureView.tsx`); coverage copy (`SearchPage.tsx`).
- **`216651e`** `feat: Phase 5 nightly incremental orchestrators (two tiers)` — `ingestion/nightly.py` (unattended: team + points) + `ingestion/matchday.py` (supervised FBref player) + `run_nightly.cmd`/`run_matchday.cmd` + tests + `CLAUDE.md` runbook & season-rollover checklist. Full design in memory `phase5-nightly-incremental.md`.

Tests at handoff: **backend 118 passed** (+5: `test_nightly.py`, `test_matchday.py`), frontend tsc clean + vitest 10 passed. All fixes verified live via the preview server.

## Immediate state / open loops
1. **PR #1 is open and unmerged** against `master`. No CI is configured on the new repo, so it won't auto-check. Merge when the user is ready.
2. **New GitHub repo, first push ever.** Whole history (up to `bd14fb8`) is now on the private remote. `.env` confirmed never tracked (only `.env.example` templates; `.gitignore` correct). The `backend/_*` scratch scripts + `handoffs/` stay local-only (untracked) by design.
3. **`master` is the default branch, no `main`.** User chose base-on-master. If they later want `main` as trunk: `git push origin master:main` + repoint default in repo settings (optional, mentioned but not done).
4. **Nightly Task Scheduler job is NOT registered** (system change, left for the user). When wanted: `schtasks /Create /TN "BetStats nightly" /TR "<repo>\backend\run_nightly.cmd" /SC DAILY /ST 06:00` (adjust time). `run_matchday` stays manual by design.

## Next session: decide the next build
Per memory `post-backfill-roadmap.md`, with Europe + Phase 5 now done, the remaining roadmap items are:
- **Internationals build** (ADR 0011 — already grilled/scoped): largest backfill (~3.5–4.5k matches, 20–30h user-run chains), no deadline. By-competition-not-by-nation, no friendlies. Purpose = player-data continuity for the all-comps window. **This is the natural next big build.**
- **League One / Two player expansion** — SET ASIDE by the user; needs `/grill-with-docs` first if revived (condensed-format spike noted in roadmap).
- **Phase 5 live verification** — genuinely blocked until ~Aug 2026 (season start); first real match day proves the end-to-end pipeline. Nothing to do now.
- **Fill in team/league data for OTHER foreign leagues** — user flagged this session they'll *eventually* want real league data for foreign clubs (currently `country=NULL`, league-less). The `/competitions` fix already makes new leagues auto-appear in the UI; a new source module would slot into `nightly.py`. Not scoped yet.

## Standing gotchas (unchanged; see prior handoff + CLAUDE.md)
- `uvicorn --reload` serves STALE code (OneDrive) — plain uvicorn, restart manually. Preview servers via `.claude/launch.json` work. **`preview_screenshot` was timing out this session** (renderer hang) — DOM tools (`preview_eval`/`preview_snapshot`/`preview_network`) worked fine; verify visually via those, not screenshots.
- Live FBref (matchday tier): VPN OFF, headful, watchdog, machine awake, PYTHONIOENCODING=utf-8. Clean-exit runs LEAK `uc_driver.exe` orphans — `run_matchday` now sweeps them, but manual chains still need `taskkill /F /IM uc_driver.exe` / `/check-tasks`. (Swept 3 orphans at the start of this session.)
- MCP `postgres` points at the wrong DB — query via backend venv.
- No FastAPI TestClient — call handlers directly; ingestion tests use rolled-back real-DB sessions or monkeypatch network calls.

## Run it
```
# API:  cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000
# UI:   cd frontend && npm run dev -- --port 5173
# tests: cd backend && .venv/Scripts/python.exe -m pytest        (118)
#        cd frontend && npx tsc --noEmit && npx vitest run       (10)
```

## Suggested skills
- **`/grill-with-docs`** — REQUIRED before the internationals build only if scope has drifted from ADR 0011 (it was grilled 2026-07-05, so likely just confirm + go). Definitely needed if L1/L2 players are revived.
- **`/gstack-review`** or **`/gstack-codex`** — a pre-merge second look at PR #1 before landing, if wanted (money-adjacent ingestion automation).
- **`/check-tasks`** — if browser windows / uc_driver ghosts linger.
- **`/handoff`** — end of next session.

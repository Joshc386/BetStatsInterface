# handoff-21/07/26,14:53

**ADR 0011 (European + international scope) is DONE, merged, and pushed.** `master` is at `be20990` on both local and `origin` — the `internationals-ingest` branch (9 commits) is fully integrated; local branch still exists but is now identical to master (safe to delete whenever, not urgent). Backend: **131 tests passing.** Next session's focus (user's stated intent): **assess player stats** — no specifics given yet, likely a data-quality/coverage pass over `player_match` now that the international scope (finals + Nations League + every qualifying campaign, six seasons) is fully populated alongside the existing league/cup/European data.

## One-line context
Personal football betting-research tool: free data → local Postgres → read-only FastAPI → React. Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md`, decisions in `docs/adr/`. **Read `memory/*.md` first** — `cups-internationals-sourcing.md` has the full qualifier-backfill closing numbers (don't re-derive), `post-backfill-roadmap.md` has the standing priority order.

## What's now fully built (don't re-scope, just verify if touching it)
- **v1 league scope**: PL/Championship/L1/L2 team data (fd.co.uk) + player data (FBref, L1/L2 best-effort).
- **Domestic cups**: FA Cup, EFL Cup, Championship Play-offs — covered ties, 6 seasons.
- **European club**: UCL/UEL/UECL covered ties, 6 seasons (UEFA Super Cup deliberately deferred).
- **Internationals — JUST COMPLETED**: World Cup, Euros, Copa América, AFCON, Asian Cup, Gold Cup, Nations League (all finals) + **World Cup Qualifiers** (single competition, all 6 confederations + inter-confed play-offs), **Euros Qualifying**, **AFCON Qualifying**, **Asian Cup Qualifying**. Totals: 2,532 fixtures, 4,912 team rows, 76,024 player rows across the international scope alone. Full per-competition breakdown + honest-gap enumeration (69 no-player-row fixtures, 7 abandoned matches, 2 competitions with a genuine zero-corners source gap) is in **ADR 0011's 4th Update section** — read that before assuming anything is missing or broken.
- Upcoming-fixtures loader (`ingestion/upcoming.py`) now also carries the World Cup with ephemeral placeholder handling (August-boundary season, TBD-slot holding, auto-purge once kicked off).

## "Assess player stats" — likely starting points (not prescribed, just where the data lives)
- `app/stats.py` — `entity_summary()` is the core rolling-window/summary-metric engine for both teams and players; `app/main.py`'s `/players/{id}/summary` is the API surface.
- `frontend/src/pages/PlayerView.tsx` — the player hub UI (scope selector, segment-by, threshold/hit-rate).
- Cross-scope sanity check worth running early: pick a heavily-capped player (Saka, id 434, was used earlier this project to verify one-profile-per-player and cross-scope appearance continuity — still a good spot-check subject) and confirm `entity_summary` aggregates cleanly now that `international` scope has ~5x the row count it did before this session.
- Known accepted data quality caveats to keep in mind while assessing (not bugs): AFCON/Asian Cup Qualifying have zero corners (source has no panel); minutes/appearances continuity is the intended signal on thin-metric international rows, not shots/tackles; L1/L2 player data is labelled best-effort.

## Standing gotchas (unchanged)
- Live FBref: VPN OFF (Surfshark adapter must say Disconnected), headful, machine awake, PYTHONIOENCODING=utf-8.
- `uvicorn --reload` serves stale code (OneDrive breaks file watching) — restart manually after backend edits.
- MCP `postgres` points at the wrong DB — query via backend venv scripts (`SessionLocal()`), not the MCP tool.
- A `.cmd` file needs CRLF + pure ASCII (the Write tool emits LF/UTF-8 by default) — convert before running.
- Background-launching a `.cmd` needs the ABSOLUTE path; bare filenames fail silently (cwd not carried into the background shell).
- A machine restart mid-chain is harmless: every ingestion path is resumable (`_already_ingested` / idempotent upserts) — just re-launch the identical command, already-done work re-verifies at near-zero network cost in seconds. Hit this twice during the qualifier backfill, both times fine.

## Run it
```
# tests:  cd backend && .venv/Scripts/python.exe -m pytest      (131)
# API:    cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000
# UI:     cd frontend && npm run dev -- --port 5173
```

## Open loops (low priority, user hasn't asked)
1. SearchPage / scope-selector coverage copy should mention qualifiers honestly (was already an open item before this session; still open).
2. Delete the now-redundant local `internationals-ingest` branch (identical to master).
3. Nightly Task Scheduler job (Phase 5) still unregistered.
4. L1/L2 player expansion still SET ASIDE (would need `/grill-with-docs` per project rule).
5. `_qual_chain_*.cmd`, `_qual_shim_poc.py`, `_qual_schedule_poc.py/.json` and similar underscore-prefixed scratch files in `backend/` are intentionally untracked ops tooling — don't add them to git without asking; they're reusable if a qualifier alias needs re-verifying later.

## Suggested skills
- **`/gstack-review`** — if a further merge/PR happens later touching ingestion; skipped for this merge since it was a direct-to-master fast-forward the user explicitly requested, but worth it before anything that changes stats/rolling-window logic (money-adjacent per the user's global rules).
- **`/diagnose`** — if the player-stats assessment turns up something that looks broken rather than a data-coverage gap.
- **`/grill-with-docs`** — if the assessment surfaces a scope question (e.g. "should qualifiers count toward international form windows the same as finals?").
- **`/handoff`** — end of next session.

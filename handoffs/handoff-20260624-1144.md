# handoff-24/06/26,11:44

**Next session focus:** decide the Championship/other-leagues path (see "Decision pending"), then either build a custom Championship scraper, test Big-5 "other leagues", or pivot to the Next.js frontend. Pick up where we left off.

---

## One-line context
Personal football betting-*research* tool: free data → local Postgres → read-only API → display. Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md`, decisions in `docs/adr/`. Read `memory/player-data-fbref-blocked.md` and `memory/postgres-access.md` first.

## What got done this session (all committed — see `git log`)
- **Phase 4 FBref player ingestion built + tested** (`a6f1cb6`): `backend/ingestion/players.py` (+ `tests/test_players.py`, 13 tests green). Recovers stable FBref player id from cached match HTML (`data-append-csv`), reconciles team names (fail-loud), idempotent commit-per-match upserts, fixture linking.
- **Watchdog supervisor** `backend/ingestion/run_backfill.py` (`a6f1cb6`, fixed in `73c68b8` + `09a59a0`): restarts the backfill subprocess on Cloudflare stalls; blocks idle-sleep via `SetThreadExecutionState`.
- **Parameterized by competition** (`09a59a0`): `run_backfill <season> "<comp>"` / `players <season> "<comp>" [limit]`; `LEAGUE_IDS` in `players.py`. Validated against `FBref.available_leagues()`.
- **Luton Town alias** (`65329f0`) for 2023-24 PL.

## Data state (Postgres `betstats`)
- **Premier League fully done: 2023-24, 2024-25, 2025-26** — ~34,400 `player_match` rows, 537+ players, 0 skips per season. PL `team_match` already complete from earlier phases.
- Championship / other leagues: **no player data yet.**

## THE blocker — Championship is not fetchable via soccerdata
Verified twice: `read_schedule("ENG-Championship")` → `ValueError: No objects to concatenate`. Root cause confirmed in `soccerdata/fbref.py:611` `read_schedule` — it iterates `read_seasons(split_up_big5=True)`, which returns **nothing** for the Championship, so the loop never runs and no page is even cached. soccerdata coverage gap, not our pipeline. The per-match player fetch would work; we just can't enumerate Championship matches without the schedule.

## Decision pending (next session starts here)
User was asked to choose; **no answer given yet**:
1. **Custom Championship scraper** — fetch FBref "Scores & Fixtures" page directly via the soccerdata Cloudflare-solved session, parse `game_id`s ourselves (reuse `parse_player_ids`-style HTML parsing), then feed the existing per-match pipeline. ~half-day build + 24-team alias pre-pass + ~7h backfill. Real path to Championship.
2. **Test Big-5 "other leagues"** — La Liga/Serie A/Bundesliga/Ligue 1 are soccerdata-supported; `read_schedule` likely works with no custom code. One quick live test confirms.
3. **Bank PL, build the Next.js frontend** — make the 3-season PL dataset usable (approved preview is the target).

Recommendation given: don't sink more time into Championship blindly — quick La Liga test (2) or pivot to frontend (3); treat the custom scraper as a separate planned task.

## CRITICAL operational rules (cost ~16h of wall-clock this session — do not relearn)
- **FBref needs the VPN OFF** (Cloudflare blocks the VPN exit IP).
- **Run season backfills as the Windows Scheduled Task `BetStatsBackfill`, NOT a Claude background bash job.** Claude background tasks die when the app is closed/backgrounded/idle for hours (this is a laptop, often on battery; AC sleep is already `0`=never). The scheduled task runs independent of the app and survives. Runner: `backend/_sched_backfill.bat` (untracked; edit its season/competition line) → redirect to `%TEMP%\betstats_sched.log` (NOT `backfill.log`, which `run_backfill.py` opens itself → Windows PermissionError on double-open). Re-trigger with PowerShell `Start-ScheduledTask -TaskName BetStatsBackfill`. **Keep the laptop on AC.**
- **Each new season/competition needs FBref team aliases pre-derived** in `FBREF_TEAM_ALIASES` (`players.py`). `link_fixtures` is fail-loud and aborts the whole season on the first unmapped team. Before a backfill, diff the season's cached FBref schedule names vs `teams.canonical_name` and add all gaps at once.
- **Claude UI "task running Nh" cards are stale ghosts** — once a run is reaped, `TaskStop` returns "No task found" and there's no live process; verify with a process scan (`Get-Process python,uc_driver,chrome`) and dismiss the card from the UI.

## Loose ends / cleanup
- `?? backend/_sched_backfill.bat` — untracked scratch runner for the scheduled task. Keep (needed) or gitignore; do not commit as-is.
- **Scheduled task `BetStatsBackfill`** still registered (state: Ready). Delete with `Unregister-ScheduledTask` when the whole backfill effort is finished.
- **~11 orphan `uc_driver.exe`** accumulated across runs. Safe to sweep now that no run is active: `Get-Process uc_driver | Stop-Process -Force` (the PL run is done; nothing live to protect).
- API gap (pre-existing): `app/stats.py` metric registry doesn't expose `goals`/`assists` yet — add when wiring the player UI.

## Suggested skills
- **/grill-with-docs** — before building the custom Championship scraper (non-trivial, money-adjacent); spec it + update `CONTEXT.md`/ADR.
- **/tdd** — for the scraper's `game_id` parsing + any new reconciliation (same discipline as `tests/test_players.py`).
- **/diagnose** — if FBref fetches hang again, reproduce→bound→fix rather than ad-hoc retries.
- **/run** or **/verify** — once the Next.js frontend exists, to confirm it renders against the live API.

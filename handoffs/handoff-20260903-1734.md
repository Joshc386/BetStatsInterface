# handoff-03/09/26,17:34

> **Read [handoff-20260901-2118.md](handoff-20260901-2118.md) first — it is the
> substantive one.** It carries the per-90 groundwork, the corrected backfill
> sizing, the two findings that inverted mid-session, and the full gotcha list.
> This document exists only to record that **nothing has changed since**, and to
> say what that silence proves.

## Status: quiet, and deliberately so

**No code was written between 01/09 21:18 and 03/09 17:34.** No commits, working
tree clean, everything pushed. The two days were spent letting the automation run
unattended after a session that changed a lot of it.

```
BetStats job digest — 03/09/2026 17:34, last 72h
no failures since 31/08 17:34 (37 run(s) checked)
```

**37 consecutive clean runs.** That is the load-bearing fact in this document,
because it is the first evidence that last session's changes hold up unattended:

- The **TBD placeholder fix** (`5220cb8`) has survived without a single
  `unresolved opponent` failure — the fault that produced six popups on 27/08.
- The **digest** (daily 09:30) has run every day and correctly reports nothing.
  Its first real test was reporting *silence*, which is the harder case.
- **No modal popups have appeared.** `notify_failure.ps1` is gone from all four
  runners and nothing regressed.
- The **ingestion backlog is 0** — no fixture is past kick-off and still
  `scheduled`, so nothing is silently un-ingested.

Current-season data kept itself current throughout: League One 2627 grew 35 → 47
fixtures and League Two 36 → 48, purely from the daily `matchday` runs.

## What is still open — unchanged from 01/09

Nothing has been picked up, so the previous handoff's list stands in full. The
three that matter:

1. **Per-90 review** — the stated next focus. The measurement is already done and
   written up in the 01/09 handoff: `average` and `per_90` divide by **different
   denominators**, and 41,825 of 317,902 player rows (13%) have minutes recorded
   with a NULL metric. Framed as a **contract question, not a bug** — settle what
   belongs in the denominator before touching code.
2. **[PR #2](https://github.com/Joshc386/BetStatsInterface/pull/2)** — OPEN,
   MERGEABLE, **12 commits, still 0 reviews**. Note its "Known gap" section is
   **stale**: Barnet, York and Scunthorpe now have `fbref_id`s, so the
   `backfill_team_fbref_id` follow-up it recommends is unnecessary. Only
   **Southend** still lacks one.
3. **Historical L1/L2 backfill — not started.** Historical seasons still hold 2–3
   fixtures each. 12 stages of 550, **~5.5h each, ~66h total** (not the old 28h
   figure — see 01/09 for why). Resumable, newest-first, each stage independently
   useful.

Open loops 4–8 in the 01/09 handoff (late slots, 336 NULL-`fdcouk_name` teams,
the 8 unjudgeable fixtures, `ESPN_PLAYER_ALIASES`, the fd.co.uk `HTTP 300`) are
all still open and all still non-urgent.

## One thing worth watching next session

`League One Play-offs` and `League Two Play-offs` still show **0 fixtures with
player data** in every season. That is expected — only League One 2526 has had
its fixtures linked, and none of them have been *ingested* yet because that
happens as part of the backfill stage for that season. It is not a regression.
Flagged only so it is not mistaken for one.

## Run it
```
# tests:    cd backend && .venv/Scripts/python.exe -m pytest       (267 passed)
# digest:   .venv/Scripts/python.exe -m ingestion.digest 72
# backfill: .venv/Scripts/python.exe -m ingestion.run_backfill 2526 "League One"
```

## Suggested skills
Unchanged from 01/09, and none has been run yet:

- **`/grill-with-docs`** — for the per-90 review, **before any code**. It is a
  statistical-contract question, and the answer belongs in `CONTEXT.md` as
  vocabulary (`per_90`, `Appearance` and `minutes_total` are already domain
  terms).
- **`/tdd`** — once the contract is agreed. Project rule for statistical code is
  a fixed-input/expected-output regression test.
- **`/gstack-review`** — on PR #2 before merging; it touches the supervised
  ingestion path. Suggested twice now and still not run.
- **NOT `/diagnose`** for per-90 — no reported bug; treating a contract question
  as a defect produces a fix for the wrong thing.
- **NOT `/run`** — the UI has not been touched.

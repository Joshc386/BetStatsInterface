# handoff-05/07/26,09:27

**Everything planned for this stretch SHIPPED and is committed: the 6-season player backfill landed + swept, the 14-duplicate-team bug was found and fixed, and the computed league table (ADR 0010) is live.** Nothing is mid-flight. The next session starts clean on the agreed roadmap: **team-form competition-scope selector**, then `/grill-with-docs` on League One/Two player expansion, then Phase 5 by early August.

## One-line context
Personal football betting-*research* tool: free data → local Postgres → read-only FastAPI → React (Vite). Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md`, decisions in `docs/adr/` (0010 is new). **Read `memory/*.md` first** — esp. `post-backfill-roadmap.md` (the agreed sequence + why-compute-not-fetch reasoning) and `duplicate-team-rows-cup-autocreate.md` (the guard semantics the next cup ingest will hit).

## What shipped (see the commits for detail — don't re-derive)
- `1cbc322` **duplicate-team fix**: cup auto-create had silently duplicated 14 in-universe L1/L2 clubs (dup rows held the fbref_id seam → would have misrouted Wigan's Champ 2223 league data). All 14 merged into canonicals via `backend/_merge_duplicate_teams.py` (untracked scratch, dry-run+--apply pattern, kept for reference); 14 aliases added; `cups.resolve_or_create_fbref_team` now FAILS LOUD when a new name's first word matches an existing team (`CUP_CREATE_ALLOWLIST` = escape hatch, currently empty).
- `069dcd9` **6-season sweep**: backfill chain finished overnight 03→04 Jul — league seasons 100% zero-skip; 11 cup ties recovered after 4 alias adds (Hartlepool United, Forest Green Rovers, Swindon Town, Oldham Athletic — the last two were the guard working). Coverage verified: every comp-season 2021→2526 populated (~10.4–11.6k player rows/PL season, ~15.2–17k/Champ). SearchPage footer updated.
- `86b182c` **league table (ADR 0010)**: `GET /table` computed from team_match (Pts/GD/GF tie-break, optional `as_of`), `points_adjustments` table (migration 0004) seeded with **14 deductions from ESPN standings** (`ingestion.points_adjustments --apply`, idempotent — re-run for new rulings), `/table` UI page with deduction footnotes + nav link. Known-output tests pinned to published finals (Everton 40, Forest 32, Derby 34).

Tests at handoff: **backend 106 passed**, frontend tsc clean + vitest 10 passed. League table verified live in preview (Championship 25/26 shows Sheffield Weds bottom on 0 pts after −18 — deductions provably applied).

## Next session, in order (per memory `post-backfill-roadmap.md`)
1. **Team-form competition-scope selector** — long-deferred, unblocked since cup team data exists. Fixture view's team-form panels currently show league-only form; the selector lets the user include/exclude scopes (club_league / club_cup). Look at `frontend/src/pages/FixtureView.tsx` (mode/venue toggles are the pattern to extend) and `/fixtures/compare` (already returns rows with `competition_id`; check whether it filters to league — that's `app/fixtures.py`). Mostly frontend; possibly an API param.
2. **`/grill-with-docs` on League One/Two player expansion** — user has verified (screenshots, 04 Jul) that FBref L1/L2 match pages carry ALL our metrics in the condensed single-table format (Fls/Fld/TklW inline — one fetch cheaper than top-flight). Grill must settle: one-page parser spike (condensed format vs our stat_type reads — FA Cup early-round pages are likely already this shape and parsed fine), season depth, whether cup `COVERED_COMPETITIONS` extends to L1/L2, CONTEXT.md coverage-label updates. Scale: ~6,600 pages ≈ ~28h fetching = multi-evening USER-RUN chain (memory: `long-runs-user-vs-agent.md`).
3. **Phase 5 nightly incremental** — must be ready by early Aug (season starts mid-Aug). ~80% built; remaining = results/player refresh orchestrator on the Task Scheduler pattern. Wire `points_adjustments` re-run into it. → memory `phase5-nightly-incremental.md`.
- (Deferred but wanted: European/internationals — own grill when taken on.)

## Standing gotchas (unchanged — full list in prior handoffs + memories)
- `uvicorn --reload` serves STALE code (OneDrive); plain uvicorn, restart manually. Preview servers via `.claude/launch.json` ("api" + "frontend") work.
- No FastAPI TestClient — call handlers directly, every `Query()` param explicit (see `tests/test_table.py::test_table_endpoint_defaults_and_guards`).
- Live FBref: VPN OFF, headful, under the watchdog. MCP `postgres` points at the wrong DB — query via backend venv.
- Next cup-season ingest (L1/L2 expansion or 2627 cups) WILL trip the first-word guard on some clubs — that's an alias to add + re-run, NOT a bug.
- New deduction rulings: `python -m ingestion.points_adjustments` (dry-run) then `--apply`.
- Untracked scratch (`backend/_*.py`, `_backfill_chain.cmd`, etc.) — pre-existing pattern; `_merge_duplicate_teams.py` and `_backfill_chain.cmd` are the reusable ones.

## Run it
```
# API:  cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000
# UI:   cd frontend && npm run dev -- --port 5173     # league table at /table
# tests: cd backend && .venv/Scripts/python.exe -m pytest   (106)
#        cd frontend && npx tsc --noEmit && npx vitest run  (10)
# deductions refresh: cd backend && .venv/Scripts/python.exe -m ingestion.points_adjustments --apply
```

## Suggested skills
- **`/grill-with-docs`** — REQUIRED before the League One/Two player expansion (item 2): new coverage scope + cup-filter decisions; updates CONTEXT.md/ADR inline.
- **`/tdd`** — any new ingestion/reconciliation logic (project rule); the scope selector's aggregation changes should extend `frontend/src/lib/aggregate.test.ts` if logic moves.
- **`/check-tasks`** — if anything looks stuck or a uc_driver/Chrome ghost lingers from old chains.
- **`/handoff`** — end of next session.

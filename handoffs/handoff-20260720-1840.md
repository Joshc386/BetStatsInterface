# handoff-20/07/26,18:40

**The qualifier backfill (ADR 0011) is mid-flight: chain 1 of 3 is DONE and audited; chain 2 (CAF + AFC + Asian Cup Qualifying) is RUNNING in the user's own terminal right now; chain 3 has not started.** The user will resume this session when chain 2 finishes — the next session's first job is the chain-2 skip triage → alias additions → cached recovery pass → audit, then hand the user chain 3. The World Cup 2026 finals are fully closed out (104/104 incl. the Jul 19 final).

## One-line context
Personal football betting-research tool: free data → local Postgres → read-only FastAPI → React. Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md` (read **Dual-badged match**), decisions in `docs/adr/0011` (three Update sections — the 2026-07-09 one holds the qualifier scope). **Read `memory/cups-internationals-sourcing.md` first.**

## State on branch `internationals-ingest` (unpushed, no PR)
- `f9bd287` qualifier plumbing (fbref_shim + WC Qualifiers model + 2020-08-01 floor)
- `1b9430d` World Cup on the upcoming slate (ephemeral placeholders, TBD-slot holding, purge)
- `ffd1159` purge-test isolation fix
- `eaf74d9` chain-1 fallout fixes (dateless-row guard, 5 nation aliases, New Caledonia allowlist)
- Backend tests **131 passed** at last full run (130 + dateless-row test).

## Data state (all audited via SQL, 2026-07-20)
- **World Cup**: 168 fixtures (64×2022 + 104×2026 incl. final), 336 team rows, 5,283 player rows. COMPLETE.
- **World Cup Qualifiers**: 904 fixtures, 27,701 player rows, 2020-10-08 → 2026-03-31, zero unrecovered skips. UEFA/CONMEBOL/CONCACAF/OFC/play-offs ×2022+2026 all in. **team_rows=0 — CORRECT: qualifier team passes run at the end of chain 3** (`_qual_chain_3.cmd` does them). 10 no-player-row fixtures = likely CONCACAF COVID forfeits — enumerate in the final audit, don't "fix".
- **Chain 2 target** (running): WC Qual CAF 2022+2026, WC Qual AFC 2022+2026, Asian Cup Qualifying 2023 (~900). **Asian Cup Qualifying's fixture count is EXPECTED below its schedule count** — dual-badged AFC matches deliberately land under WC Qualifiers (chain order enforces it; see CONTEXT.md).
- **Chain 3** (not started): `_qual_chain_3.cmd` — Euros Qualifying 2020+2024, AFCON Qualifying 2021/2023/2025/2027, then team passes for all 4 qualifier comps.

## The proven chain-followup playbook (used for chain 1 — reuse verbatim)
1. While a chain runs, a Monitor tails `backend/backfill.log` for `watchdog|SKIP|skipped=|games=|Traceback|Error|MAX_RESTARTS` (grep needs `--line-buffered`). Skips are COUNTED not acted on mid-chain.
2. After the chain: get exact schedule spellings offline — no-op the webdriver (`BaseSeleniumReader._init_webdriver = lambda self: None`), `read_schedule(force_cache=True)` via `ingestion.fbref_shim.FBref`, diff schedule names vs the player-df name in the error.
3. Add `FBREF_TEAM_ALIASES` entries (player-df FULL → schedule SHORT, watch en-dashes) in `players.py`; genuinely-distinct guard-token clashes go to `CUP_CREATE_ALLOWLIST` in `cups.py` (New Caledonia precedent).
4. Recovery = re-run each edition with skips via `run_backfill` — skipped pages are cached, near-zero network. Chain the legs `&&` in ONE background bash command (`cd` + `./.venv/Scripts/python.exe -u -m ingestion.run_backfill <ed> "<selector>"`).
5. Sweep `uc_driver.exe` orphans after (recovery runs leak them; chain .cmds sweep themselves).
6. Commit fixes as one `fix:` commit.

## Chain-1 lessons already baked in (don't rediscover)
- **Stale current-season schedule cache**: soccerdata deemed WC-2026 "complete" and reused a Jul-10 schedule snapshot; fix = delete `~/soccerdata/data/FBref/schedule_<league>_<season>.html` and re-run. Watch for the same on AFCON Qualifying 2027 / Asian Cup Qualifying 2023 if their counts look short.
- **Dateless schedule rows** (cancelled matches, e.g. Tonga–Cook Islands): now guarded in `select_all_games` — a `games=` count one short of the page is that guard working.
- Background-launching a `.cmd` needs the ABSOLUTE path (`cmd //c "C:\...\x.cmd"`); bare names fail (cwd not carried).
- The `1/25` in the user's console is watchdog attempt 1 of MAX_RESTARTS — progress lives in `backfill.log` only.

## Open loops after chain 3
1. **Final all-chains audit**: per-league fixtures vs schedule counts (WC Qualifiers audit = SUM of 8 league schedules vs the one comp), enumerate all no-player-row fixtures as real-world anomalies, corners coverage on team rows.
2. **UI coverage copy** — SearchPage/scope selector should mention qualifiers honestly (handoff item from 2026-07-08, still open).
3. **Memory + ADR 0011 update** with final qualifier numbers (follow the finals-backfill precedent).
4. **Merge/PR `internationals-ingest`** — now 4+ commits; suggest `/gstack-review` pre-merge (money-adjacent ingestion). User hasn't asked yet.
5. Upcoming-slate placeholders: WC is over — the daily 07:30 task purges kicked-off placeholders itself; nothing to do unless ghosts appear in `/fixtures/upcoming`.
6. Nightly Task Scheduler job (Phase 5) still unregistered; L1/L2 players still SET ASIDE.

## Standing gotchas (unchanged)
- Live FBref: VPN OFF (check Surfshark adapter says Disconnected), headful, machine awake, PYTHONIOENCODING=utf-8. Never run two live FBref processes concurrently (per-process rate limiter).
- `uvicorn --reload` serves stale code (OneDrive); preview servers via `.claude/launch.json`.
- MCP `postgres` points at the wrong DB — query via backend venv scripts.
- `backfill.log` is append-only across all chains — always window by the latest `===== watchdog` header, never grep the whole file.
- Write tool emits LF; `.cmd` files need CRLF (+ pure ASCII to be safe).

## Run it
```
# tests:  cd backend && .venv/Scripts/python.exe -m pytest      (131)
# API:    cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000
# UI:     cd frontend && npm run dev -- --port 5173
# chain progress:  Get-Content backend\backfill.log -Tail 5 -Wait
```

## Suggested skills
- **`/check-tasks`** — first thing on resume: verify no ghost uc_driver/chrome processes before recovery runs.
- **`/gstack-review`** — pre-merge review of `internationals-ingest` once chain 3 + audit land.
- **`/handoff`** — end of next session if chain 3 spills over.

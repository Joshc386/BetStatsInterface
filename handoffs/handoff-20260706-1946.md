# handoff-06/07/26,19:46

**European club coverage went from scoped-on-paper to DONE today: ADR 0011 written (grill session), the `club_european` pipeline built (`45ac72d`), the full 6-season backfill run + swept (502 fixtures = every covered tie, 15,292 player rows, 1,002 team rows), and Europe wired into the fixture-view scope selector (`bd14fb8`).** Nothing is mid-flight. The user wants the next session to start with **a review of the latest state of the app**, then decide the next build.

## One-line context
Personal football betting-*research* tool: free data → local Postgres → read-only FastAPI → React (Vite). Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md` (updated this session: Covered tie generalised, all-competitions window legitimised, Covered international competition added), decisions in `docs/adr/` (0011 is the big new one — scope + landed-ingestion update). **Read `memory/*.md` first** — esp. `cups-internationals-sourcing.md` (full European end-state + ops lessons) and `post-backfill-roadmap.md` (what's next and why).

## What shipped today (commits carry the detail — don't re-derive)
- `f6f9432` team-form **competition-scope selector** (repeated `scope` param on `/fixtures/compare`; League/Cups toggle).
- `7491e5a` **ADR 0011**: club_european = covered ties only (UCL/UEL/UECL/Super Cup, CWC excluded as form noise, 6 seasons); internationals = player-data continuity, by-competition-not-by-nation, no friendlies. L1/L2 players set aside; Europe → internationals prioritised.
- `45ac72d` **club_european pipeline**: migration 0005 (`fixtures.stage` joins the natural key — European rematches repeat an orientation within a season), 4 comps seeded, EUR-* league_dict entries, cups.py knockout-generic (stage threading, country-code split, first-non-generic-token guard).
- `533350f` + `bd14fb8` **backfill landed**: AEK → generic guard tokens; 10 two-spelling aliases (schedule SHORT vs player-df FULL — the cup-era seam at continental scale); ADR Super Cup deferral; scope selector gains **All comps / Europe** (verified live: City's Europe window = pure UCL form).

Tests at handoff: **backend 113 passed** (includes 2 new fixtures-scope + 5 European tests), frontend tsc clean + vitest 10 passed. All DB regression guards green.

## Next session: review the latest state of the app (user's stated intent)
1. Run both servers (commands below) and walk the surfaces: fixture view (all 5 scope options incl. Europe on a Big-Six fixture — e.g. `/fixture/54/vs/6` City–Villa), H2H (European meetings now appear in the meetings list automatically), squad form, `/table`, search.
2. Things worth eyeballing for review polish: the scope selector now has 5 options (crowding? mobile?); European rows in H2H carry stage-distinct duplicates of the same pairing (Benfica-style) — check display reads sensibly; foreign clubs have `country=NULL` and no league data (their team pages will look sparse — that's by design, "covered competitions only", but check it *reads* honestly in the UI).
3. Then decide next build order: **Phase 5 nightly incremental (hard deadline: ready by early Aug, season starts mid-Aug — ~80% built, see memory `phase5-nightly-incremental.md`; must now also cover European match days + `points_adjustments` re-runs)** vs **internationals build (ADR 0011, largest backfill ~3.5–4.5k matches over multiple evenings, no deadline)**. Prior agent's recommendation: Phase 5 first — deadline beats no-deadline.

## Standing gotchas (new ones from today in bold)
- **Clean-exit ingest runs leak `uc_driver.exe` orphans** (59 accumulated during the chain day; each holds a Chrome window). Sweep with `taskkill /F /IM uc_driver.exe` after chain evenings, or `/check-tasks`.
- **UEFA Super Cup is deferred** — soccerdata's `read_schedule` crashes on its single-match page; 3 covered matches, hand-seedable (ADR 0011 update). Don't "fix" this without re-reading the ADR.
- **`_europe_sweep.cmd` includes a 2021 Conference League run that can never succeed** (comp founded 2021-22) — wasted watchdog cycle if re-run; harmless.
- `uvicorn --reload` serves STALE code (OneDrive) — plain uvicorn, restart manually. Preview servers via `.claude/launch.json` work.
- Live FBref: VPN OFF, headful, always under the watchdog; PYTHONIOENCODING=utf-8 for European output (cp1252 crashes on foreign characters).
- MCP `postgres` points at the wrong DB — query via backend venv.
- No FastAPI TestClient — call handlers directly, every `Query()` param explicit.
- Untracked scratch pattern: `backend/_*.py`, `_*.cmd` (chain/sweep scripts kept for reference).

## Run it
```
# API:  cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000
# UI:   cd frontend && npm run dev -- --port 5173
# tests: cd backend && .venv/Scripts/python.exe -m pytest        (113)
#        cd frontend && npx tsc --noEmit && npx vitest run       (10)
```

## Suggested skills
- **`/gstack-design-review`** or **`/gstack-qa`** — the app-state review the user asked for: 5 surfaces, new Europe scope, worth a designer's-eye/QA pass now that data breadth outgrew the UI it was designed for.
- **`/check-tasks`** — if browser windows or uc_driver ghosts linger from today's chains.
- **`/grill-with-docs`** — NOT needed for internationals (ADR 0011 already settled it); only if the review changes scope.
- **`/handoff`** — end of next session.

# handoff-03/07/26,18:30

**Four things shipped this session (all committed, all green); a fifth — the 6-season historical player backfill — is MID-FLIGHT, being run BY THE USER via `backend/_backfill_chain.cmd`.** The user will open the next session with roughly: *"the backfill chain finished — sweep backfill.log for failures, fix any aliases, re-run the aborted seasons, then verify coverage and run the tests."* That sweep is this handoff's main job.

## One-line context
Personal football betting-*research* tool: free data → local Postgres → read-only FastAPI → React (Vite). Rules in `CLAUDE.md`, vocabulary in `CONTEXT.md`, decisions in `docs/adr/` (0009 is new). **Read `memory/*.md` first** — esp. the new `long-runs-user-vs-agent.md` (user directive on who runs long scripts) and updated `cups-internationals-sourcing.md`, `fdcouk-corner-errors.md`, `frontend-phase7.md`, `phase5-nightly-incremental.md`.

## What shipped this session (committed — see the commits for detail)
- `3d18933` **cup + play-off corners**: the "FBref cup pages lack the corners panel" premise was FALSE (368/402 cached pages have it). `players.parse_corners` → 766/834 club_cup team rows now carry corners (68 NULLs = 34 FA Cup R3 fixtures, genuinely absent upstream). Play-offs got their first 30 team_match rows. Parser cross-validated vs fd.co.uk on 2,795 league fixtures (95.7% exact; ±1s = provider variance).
- `25c809d` **fd.co.uk corner corrections**: 5 third-source-confirmed bad CSV cells overridden at ingest via `team_match.CSV_CORRECTIONS` (fail-loud, survives re-ingestion). KEY LESSON: FBref alone is NOT evidence — ESPN sided with fd.co.uk on Blackburn–Birmingham. Only third-source-confirmed corrections get registered.
- `dae1e25` **upcoming fixtures via ESPN (ADR 0009)** + landing-page slate + **H2H now spans cups** (scope purity binds Rolling Windows, not the meetings list — CONTEXT.md updated). 159 real 2026-27 fixtures ingested; `teams.espn_id` seam live (6 aliases converged); York seeded (promoted clubs = deliberate summer-prep seed, never auto-created). fd.co.uk `fixtures.csv` REJECTED for upcoming (1-week horizon, no cups).
- `3252768` **daily automation**: Task Scheduler task "BetStats upcoming fixtures" → `backend/run_upcoming.cmd` daily 07:30 → `backend/logs/upcoming.log`. Verified end-to-end through the scheduler.

Tests at handoff: **backend 100 passed**, frontend tsc clean + vitest 10 passed. Fixture view + slate verified live in the preview (screenshots taken; H2H comp filter chips work — Liverpool/Chelsea shows PL+EFL Cup).

## THE MID-FLIGHT WORK — historical player backfill (3 → 6 seasons)
Goal: player data for seasons **2021/2122/2223** for PL, Championship, FA Cup, EFL Cup (matching the 6-season team-data depth), plus zero-network cup/play-off team+corners passes for those seasons.

**State when the user takes over:**
- PL **2223 COMPLETE** (380/380, 11,345 rows/554 players, quality gates green).
- PL **2122 PARTIAL** (was mid-run when handed over; chain resumes it first).
- Everything else pending, sequenced in `backend/_backfill_chain.cmd` (untracked scratch, fully idempotent/resumable — safe to re-run whole).

**Next session's sweep, concretely:**
1. Read the tail + grep `backend/backfill.log` for `UnknownTeamError` / `MAX_RESTARTS` / `no progress` / `Traceback` and the per-season summary lines (`linked=… ingested=… skipped=…`).
2. For each `UnknownTeamError`: add the FBref spelling → canonical entry to `FBREF_TEAM_ALIASES` in `ingestion/players.py` (deterministic, both-spellings rule — see the two-spelling-seam notes in `memory/cups-internationals-sourcing.md`), then re-run just that season via `python -m ingestion.run_backfill <season> "<comp>"` (VPN off, watchdog handles the rest).
3. Confirm the cup TEAM passes at the chain's end ran (`python -m ingestion.cups team <season> <cup>` × FA/EFL/Play-offs × 2021/2122/2223); re-run any that didn't (zero-network, idempotent).
4. Verify: coverage query (player_match rows/players per competition-season — expect ~11.4k rows/PL season, ~16.8k/Champ season, cups proportional to covered ties), full pytest, then commit (likely just alias additions + docs/memory updates; the data itself isn't committed).
5. Update `memory/cups-internationals-sourcing.md` (or a new memory) with the final 6-season state; consider promoting `_backfill_chain.cmd` knowledge into CLAUDE.md if deeper history ever recurs.

**Known risks for the sweep:** older Championship seasons (esp. 2021: Wycombe, Rotherham, etc.) and older cup draws are where alias work will surface; cup seasons auto-create lower-league opponents (fine, ADR 0007/0008 pattern). If FBref 2021-era pages are shaped differently (not expected — Opta era, and the Jan-2026 loss was xG-only, which isn't ingested), that shows up as parse errors, not aliases — investigate before hammering restarts.

## Standing gotchas (unchanged)
- Live FBref: **VPN OFF, headful, under the watchdog** (`ingestion/run_backfill.py`); never one-shot a cup fetch. → `memory/player-data-fbref-blocked.md`.
- `uvicorn --reload` serves STALE code (OneDrive breaks watchfiles) — plain uvicorn, manual restart. → `memory/uvicorn-reload-stale.md`.
- No FastAPI TestClient (`httpx` absent) — call handlers directly, every `Query()` param explicit.
- MCP `postgres` points at the `postgres` DB, not `betstats` — query via the backend venv (creds in `backend/.env`).
- `/fixtures/upcoming` must stay declared BEFORE `/fixtures/{fixture_id}` in `app/main.py` (literal-path ordering).
- Untracked scratch (`backend/_*.py`, `_backfill_chain.cmd`, `_sched_backfill.bat`, old handoffs) — pre-existing pattern, safe to ignore; `_backfill_chain.cmd` is the one currently load-bearing.

## Deferred / backlog (post-sweep candidates)
- **League One / Two player data** — user is interested (asked about it this session); needs a FBref coverage spike + `/grill-with-docs` first. Cup bonus coverage already gives ~1,300 distinct players/season incl. lower-league.
- **Phase 5 nightly incremental** — ~Aug 2026; upcoming-fixtures half of Job B is DONE + automated; remaining = results/player refresh orchestrator extending the Task Scheduler pattern. → `memory/phase5-nightly-incremental.md`.
- **European / internationals scopes** — deferred. → `memory/cups-internationals-sourcing.md`.
- **League table** (buildable from team_match), team-form competition-scope selector (unblocked by cup team data), Millwall–Sheffield Weds corners (unverifiable, left as-is).
- SearchPage footer data-note ("Player data: Premier League, 3 seasons") is stale — update once the 6-season backfill lands.

## Run it
```
# API:  cd backend && .venv/Scripts/python.exe -m uvicorn app.main:app --port 8000   (manual restart; NOT --reload)
# UI:   cd frontend && npm run dev -- --port 5173
# tests: cd backend && .venv/Scripts/python.exe -m pytest
# backfill (user-run, resumable):  backend\_backfill_chain.cmd   (VPN off, plugged in)
# single season re-run:  .venv/Scripts/python.exe -m ingestion.run_backfill 2021 "Championship"
# upcoming slate refresh: automated daily 07:30 (Task Scheduler) — log: backend/logs/upcoming.log
```

## Suggested skills
- **`/check-tasks`** — FIRST if anything looks stuck or a Chrome/uc_driver ghost lingers from the chain.
- **`/diagnose`** or **`/gstack-investigate`** — if a season fails in a non-alias way (parse errors on 2021-era pages, repeated no-progress exits).
- **`/grill-with-docs`** — before starting League One/Two player expansion or European/internationals (new sourcing decisions).
- **`/tdd`** — any new ingestion/reconciliation logic (project rule).
- **`/handoff`** — end of the sweep session.
